# ПЛАН ПО ВЫПОЛНЕНИЮ ЗАДАНИЯ (Задание.md)

## ФАЗА 1: БЫСТРЫЕ ФИКСЫ
- Замена печенек на насыщение (`AchievementManager.java:298`)
- Длительность счастья 100→60 мин (`AchievementManager.java:304`)
- Скорость текста при начале сезона (`CalibrationManager.java:36-48`)
- Текст "Уныние" → "Безымянный" (`PlayerEventHandler.java:55`)

## ФАЗА 2: МЕХАНИКА НОЖНИЦ
- Голова в инвентарь состригшему
- Если инвентарь полон — летит в сторону состригшего
- Красные частицы при состригании

## ФАЗА 3: ПОРТАЛЬНАЯ МЕХАНИКА
- Портал можно зажечь
- При входе: откидывание назад + слепота + замедление + фраза в чат
- Портал ломается после попытки входа
- Файл: `MixinServerPlayerEntity.java`, `Project3Mod.java`

## ФАЗА 4: ЕДА И СЧАСТЬЕ
- При поедании 30 любой еды — восстановление ячейки счастья
- Счётчик через статистику или кастомный

## ФАЗА 5: БЕЗЫМЯННЫЙ ЭФФЕКТ ПРИ 0 СЧАСТЬЯ
- Уныние (Gloom) остаётся как есть — НЕ ТРОГАТЬ
- Когда шкала счастья достигает 0 — давать не "Уныние", а **"Безымянный" (Unnamed Effect)**
- При джойне на 0 счастья — писать "§7Состояние: §4§lБезымянный"

## ФАЗА 6: УВЕЛИЧЕНИЕ ШАНСОВ НА БАГИ
- 2% → 5-8% (зависит от прогресса/уровня уныния)
- Файл: `Project3Mod.java:307`

## ФАЗА 7: ИСПРАВЛЕНИЕ ДОСТИЖЕНИЙ
- Проверить и починить: ach_29 (enchant 30), ach_35 (name tag), ach_38 (terracotta→ceramics), ach_39 (brush), ach_41 (buried treasure), ach_46 (rails+minecarts), ach_47 (trial vault), ach_49 (pale wood), ach_59 (4 banner patterns), ach_64 (name), ach_65 (igloo), ach_72 (mace effects)
- Переименовать "Отблеск науки" → "Белый снег. Серый лёд"

## ФАЗА 8: НОВЫЕ ДОСТИЖЕНИЯ
- Добавить недостающие из задания (номера 32, 33, 48 и др.)

## ФАЗА 9: АДСКИЕ ДОСТИЖЕНИЯ
- Новая цепочка ach_hell_01 — ach_hell_35+
- Убийство мобов, крафт блоков, торговля с пиглинами

## ФАЗА 10: АДСКОЕ СОБЫТИЕ (Слепота + Землетрясение)

### Исследование — существующие реализации в коде
В проекте уже есть `CameraRotatePayload(float deltaYaw, float deltaPitch)`. Сервер шлёт его, клиент крутит камеру:
```java
player.setYaw(player.getYaw() + payload.deltaYaw());
player.setPitch(Math.max(-90f, Math.min(90f, player.getPitch() + payload.deltaPitch())));
```
Сейчас используется только при окончании счастья — 10 тиков дерганья.
Это **простое дерганье**, НЕ землетрясение.

### Предлагаемая архитектура

#### 1. Новый пакет: `EarthquakePayload(int durationTicks, float intensity)`
Сервер шлёт ОДИН раз при старте адского события. Дальше всё на клиенте.

#### 2. Клиентский `EarthquakeHandler` (новый класс)
Локально в каждом клиентском тике вычисляет и применяет смещение камеры:

```
каждый тик (пока duration > 0):
  progress = duration / totalDuration       // 1.0 → 0.0
  decay = progress * progress               // квадратичное затухание

  // Основной толчок (горизонталь, axis-aligned)
  primary = sin(t * 0.3) * intensity * decay
  
  // Вторичный толчок (другая фаза, другая частота)
  secondary = sin(t * 0.45 + 1.7) * intensity * 0.6 * decay
  
  // Вертикальная составляющая (дрожь)
  vertical = sin(t * 0.8 + Math.PI) * intensity * 0.4 * decay
  
  // Случайные резкие импульсы (афтершоки)
  if (random.nextInt(100) < 3) {
    primary += (random - 0.5) * intensity * 2 * decay
    vertical += (random - 0.5) * intensity * decay
  }

  yaw += primary
  pitch += vertical
  // Поперечная тряска (roll через смещение по X/Z — если mod поддерживает)
```

**Ключевые параметры землетрясения:**
- Первичная волна: низкая частота (0.3), высокая амплитуда — медленные сильные толчки
- Вторичная волна: средняя частота (0.45), фаза сдвинута — наложение волн
- Вертикаль: высокая частота (0.8) — мелкая дрожь
- Затухание: квадратичное (сначала резко, потом затихает)
- Случайные афтершоки: 3% шанс на резкий импульс

#### 3. Визуальные эффекты (серверные)
Каждый тик адского события сервер делает:
- **Слепота**: `player.addStatusEffect(new StatusEffectInstance(BLINDNESS, 20, 0))` — 1 сек, постоянно обновляется
- **Частицы**: спавн `ParticleTypes.BLOCK` (камень, гравий, адский камень) вокруг игрока — имитация осыпающихся блоков
- **Звук**: `SoundEvents.BLOCK_STONE_BREAK`, `SoundEvents.ENTITY_GENERIC_EXPLODE` с рандомными питчами
- **Чат**: каждые N секунд сообщения вроде "§4Подземные толчки усиливаются...", "§cЗемля дрожит..."

#### 4. Возможные улучшения
- **Позиционный сдвиг**: раз в несколько тиков микро-телепорт игрока на 0.1-0.3 блока в рандомном направлении (имитация того, что его реально трясёт)
- **FOV колебания**: небольшие изменения FOV синхронно с толчками
- **Тряска инвентаря**: иконки в инвентаре слегка смещаются (через Mixin в `InventoryScreen`)

## ФАЗА 11: ТЕКСТУРЫ HUD
- Сместить аватарку с текстом вниз и вправо
- Перевернуть нижние внутренние углы рамки
- Расширить рамку под предметом
- Сместить "Прогресс" вниз и расширить шкалу

---

## ПОРЯДОК ВЫПОЛНЕНИЯ
1. Фаза 1 — быстрые фиксы
2. Фаза 2 — ножницы
3. Фаза 7 — фиксы достижений
4. Фаза 4 — еда и счастье
5. Фаза 5 — Безымянный эффект
6. Фаза 6 — шансы на баги
7. Фаза 3 — порталы
8. Фаза 8 — новые достижения
9. Фаза 9 — адские достижения
10. Фаза 10 — адское событие
11. Фаза 11 — текстуры
